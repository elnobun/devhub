// Initial User state
const initialState = {
  isAuthenticated: false,
  user: {}
};

export default (state = initialState, action) => {
  switch (action.type) {
    // Register new user

    // Default state
    default:
      return state;
  }
};
